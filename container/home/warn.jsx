import React, { useState } from 'react';
import { Container, Image, Card, Row, Col } from 'react-bootstrap';
import { FaChartArea, FaChartBar, FaChartLine, FaChartPie } from 'react-icons/fa';

export default function Warn(props) {
    console.log(props);
    return (
        <>
            <Row>
                <Col>
                    {/* {menuItems?.map(({ key, name }) =>
                        <Dropdown.Item css={{ padding: '0px' }} key={key}> <Row css={{ padding: '25px' }} onClick={() => setUser({ ...user, team: key })} > {name}</Row></Dropdown.Item>
                    )} */}
                </Col>
            </Row>
        </>
    );
}
